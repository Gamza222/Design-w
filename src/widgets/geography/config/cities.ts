// Города на карте РФ (макет 06). Координаты x/y — в системе viewBox карты (см. russia-map.ts:
// "0 256 1024 545"), т.е. x ∈ [0..1024], y ∈ [256..801]. Силуэт — стилизованная «дуговая»
// проекция (восток задран вверх, самая нижняя точка — Тува), поэтому координаты не проекция
// lat/lon, а визуальная калибровка по ориентирам силуэта: Финский залив, кавказский «клюв»
// (юг ~(60,794)), залив Петра Великого (~(764,750)), низ Тувы (480,816), мыс Лопатка (918,788).
// hub — узел, из которого расходятся маршруты (Москва). anchorEnd — подпись слева от точки
// (чтобы текст не уезжал за край карты / не ложился на соседей). labelDy — вертикальный сдвиг
// подписи в единицах viewBox (разводит слипающиеся пары подписей, точка остаётся на месте).
// Отображаемые имена — в i18n: `home.geography.cities.<id>` (ru + en).

export interface City {
  id: string;
  x: number;
  y: number;
  hub?: boolean;
  anchorEnd?: boolean;
  labelDy?: number;
}

export const CITIES: City[] = [
  { id: 'spb', x: 36, y: 564 },
  // Подпись вправо: слева от нового x=95 она уезжала за край карты (мобильный крупный шрифт).
  { id: 'moscow', x: 95, y: 612, hub: true },
  { id: 'krasnodar', x: 68, y: 745 },
  // Сочи чуть ниже — стопка «Краснодар/Сочи» стояла впритык строка к строке.
  { id: 'sochi', x: 70, y: 775, labelDy: 6 },
  // Казань чуть ниже Екатеринбурга (и по широте так) — заодно подписи не слипаются.
  { id: 'kazan', x: 168, y: 634 },
  { id: 'ekb', x: 242, y: 614 },
  { id: 'nsk', x: 382, y: 688, anchorEnd: true },
  // Иркутск — под точку, Владивосток — над: на одной строке подписи читались как
  // «Иркутск Владивосток» (теснее всего на 1280, где карта минимальная).
  { id: 'irkutsk', x: 535, y: 755, labelDy: 20 },
  { id: 'vlk', x: 772, y: 742, anchorEnd: true, labelDy: -16 },
];
